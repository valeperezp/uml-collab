spring:
  application:
    name: uml-collab-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:postgres}

  jackson:
    default-property-inclusion: non_null

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

server:
  port: ${SERVER_PORT:8080}

umlcollab:
  jwt:
    secret: ${JWT_SECRET:change-this-super-secret-key-for-uml-collab-dev-only-please-rotate}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:720}
  lock:
    ttl-seconds: ${LOCK_TTL_SECONDS:20}
  # La seleccion de proveedor de IA (Gemini > Anthropic > OpenAI, segun cual
  # tenga api-key no vacia) se resuelve en Java (ver AiClient), no aca, para
  # no depender de defaults de YAML anidados que Spring no encadena bien
  # cuando docker-compose define la variable igual con valor vacio.
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4210}

---
spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5442/uml_collab}
    username: ${DB_USERNAME:uml_collab}
    password: ${DB_PASSWORD:uml_collab}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

---
spring:
  config:
    activate:
      on-profile: dev-h2
  datasource:
    url: jdbc:h2:mem:umlcollab;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
